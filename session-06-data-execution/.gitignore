__pycache__/
*.pyc
.pytest_cache/
# model weight blobs regenerate on every `python run_demo.py`; keep the repo lean.
# checkpoint *.json metadata (step, hashes, offsets, sampler state) IS committed.
submission_artifacts/**/*.model.bin
